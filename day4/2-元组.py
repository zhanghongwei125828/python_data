# 元组 只读列表, 可循环,可切片
# 但是元组里面的列表是可以增删改查的
tu = ('张三',1,2,3,['a','b','c'])
print(tu)   #打印结果 ('张三', 1, 2, 3, ['a', 'b', 'c'])
tu1 = tu[4].append('d')
print(tu)   #打印结果 ('张三', 1, 2, 3, ['a', 'b', 'c', 'd'])

